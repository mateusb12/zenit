{
  "name": "zenit",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "deploy": "npm run lint && npm run build && git push origin main && sleep 5 && gh run watch $(gh run list --workflow deploy-github-pages.yml --branch main --limit 1 --json databaseId -q '.[0].databaseId') --exit-status",
    "deploy:trigger": "gh workflow run deploy-github-pages.yml --ref main",
    "deploy:watch": "gh run watch $(gh run list --workflow deploy-github-pages.yml --branch main --limit 1 --json databaseId -q '.[0].databaseId') --exit-status"
  },
  "dependencies": {
    "next": "16.2.4",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "repomix": "^1.14.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.4",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
